import { IfRulesDirective } from './if-rules.directive';

describe('IfRulesDirective', () => {
  it('should create an instance', () => {
    const directive = new IfRulesDirective();
    expect(directive).toBeTruthy();
  });
});
